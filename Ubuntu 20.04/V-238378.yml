---

- set_fact:
    stig_id: V-238378
    stig_text: 'FAILED. Ubuntu does not have system commands group-owned by root.'

- local_action: lineinfile regexp='^V-238378' path="{{ output_path }}" state=absent

- name: verify that the "{{ target_file }}" file is exist.
  block: 

    - name: verify the target files
      become: true
      stat:
        path: "{{ item }}"
      loop:
        - /bin/
        - /sbin
        - /usr/bin
        - /usr/sbin
        - /usr/local/bin
        - /usr/local/sbin
      register: files

    - name: Verify the following files group-owned by root
      loop: |
        {{ files.results | map(attribute='item')
        | zip(files.results | map(attribute='stat.mode'))
        | list }}

      assert:
        that: item.1 == 'root'
      loop_control:
        label: "{{ item.0 }}"

    - set_fact:
        stig_text: "PASSED"

  rescue:

    - name: change the files group-ownership to root.
      become: true
      file:
        path: "{{ item.item }}"
        group: root
        state: "{{ 'file' if item.stat.exists else 'touch' }}"
      loop: "{{ files.results }}"
      register: file_perms_rule 
  
    - set_fact:
        stig_text: "PASSED"
      when: file_perms_rule.changed | bool

    - debug:
        msg: "{{ stig_id }} {{ stig_text }}"

  always:
    - local_action: lineinfile line="{{ stig_id }} {{ stig_text }}" path="{{ output_path }}" create=yes
