---

- set_fact:
    stig_id: V-75509

- local_action: lineinfile regexp='^V-75509' path="{{ output_path }}" state=absent

- set_fact:
    stig_text: "{{ stig_id }} FAILED. unencrypted partitions detected."

- local_action: lineinfile line="{{ stig_text }}" path="{{ output_path }}" create=yes