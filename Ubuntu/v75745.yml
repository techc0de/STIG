---

- set_fact:
    stig_id: V-75745
    stig_text: 'FAILED. the os is not configured to audit the execution of the "truncate" system call'
    target_file: /etc/audit/audit.rules

- local_action: lineinfile regexp='^V-75745' path="{{ output_path }}" state=absent

- name: verify the os is configured to audit the execution of the "truncate" system call
  block: 

    - name: check for the file
      become: true
      stat:
        path: "{{ target_file }}"
      register: rules_file

    - fail: 
        msg: missing audit rules file
      when: rules_file.stat.exists == False

    - name: check for audit rules param
      become: true
      shell: "grep -iw truncate {{ target_file }}"
      register: audit_check
      failed_when: audit_check.rc != 0

    - assert:
        that: 
          - audit_check.stdout.find('#') == -1
          - audit_check.stdout.find('-a always,exit -F arch=b64 -S truncate -F exit=-EPERM -F auid>=1000 -F auid!=4294967295 -k perm_access') != -1
          - audit_check.stdout.find('-a always,exit -F arch=b64 -S truncate -F exit=-EACCES -F auid>=1000 -F auid!=4294967295 -k perm_access') != -1
        msg: AUDIT RULES NOT CONFIGURED

    - set_fact:
        stig_text: "PASSED"

  rescue:

    - name: remove any existing "truncate" rules from audit file
      become: true
      lineinfile:
        path: "/etc/audit/rules.d/truncate.rules"
        regexp: '^.*truncate.*'
        state: absent
        create: yes

    - name: configure the Ubuntu operating system to audit the execution of the 'truncate' system call, by adding the following lines to '/etc/audit/audit.rules'
      become: true
      lineinfile:
        path: "/etc/audit/rules.d/truncate.rules"
        line: |
          -a always,exit -F arch=b64 -S truncate -F exit=-EPERM -F auid>=1000 -F auid!=4294967295 -k perm_access
          -a always,exit -F arch=b64 -S truncate -F exit=-EACCES -F auid>=1000 -F auid!=4294967295 -k perm_access
        state: present
        create: yes
        owner: root
        group: root
        mode: 0640
      register: config_updated

    - set_fact:
        stig_text: "PASSED"
      when: config_updated.failed == false

    - debug:
        msg: "{{ stig_id }} {{ stig_text }}"

    - name: restart audit service 
      become: true
      shell: systemctl restart auditd.service
      ignore_errors: yes

  always:

    - local_action: lineinfile line="{{ stig_id }} {{ stig_text }}" path="{{ output_path }}" create=yes
