---

- set_fact:
    stig_id: V-100593
    stig_text: "FAILED. must be verified manually"

- local_action: lineinfile regexp='^V-100593' path="{{ output_path }}" state=absent
- local_action: lineinfile line="{{ stig_id }} {{ stig_text }}" path="{{ output_path }}" create=yes

