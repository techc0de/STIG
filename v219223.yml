---

- set_fact:
    stig_id: V-219223
    stig_text: "FAILED. Ubuntu does not generate audit records for all account creations, modifications, disabling, and termination events that affect /etc/shadow."
    target_file: /etc/audit/rules.d/stig.rules

- local_action: lineinfile regexp='^V-219223' path="{{ output_path }}" state=absent

- name: Check for stig.rules config file existence
  block: 

    - name: check for the file
      stat:
        path: "{{ target_file }}"
      register: rules_file

    - name: check for audit rules
      shell: auditctl -l | grep shadow
      register: audit_check
      failed_when: audit_check.rc != 0

    - assert:
        that: 
          - audit_check.stdout_lines.find('#') == -1
          - audit_check.stdout_lines.find('-w /etc/shadow -p wa -k usergroup_modification') != -1
        fail_msg: Audit Rules Not Configured

    - set_fact:
        stig_text: "PASSED"

  rescue:

    - name: remove any existing "/etc/shadow" rules from audit file      
      lineinfile:
        path: "{{ target_file }}"
        regexp: '^(.*)shadow(.*)$'
        state: absent
        create: yes
        backup: yes

    - name: add/update the audit rules for '/etc/shadow'.
      lineinfile:
        path: "{{ target_file }}"
        line: |
          -w /etc/shadow -p wa -k usergroup_modification
        state: present
        create: yes
        owner: root
        group: root
        mode: 0640 
      register: file_updated

    - set_fact:
        stig_text: "PASSED"
      when: file_updated.failed == false

    - name: reload the rules file
      shell: augenrules --load
      register: reload_rules

    - debug:
        msg: "{{ stig_id }} {{stig_text}}"

  always:
    - local_action: lineinfile line="{{ stig_id }} {{ stig_text }}" path="{{ output_path }}" create=yes



